# Expo/React Native dependency trees frequently contain optional peer conflicts
# (misalnya react-dom vs react). Gunakan resolusi peer yang permisif seperti yang
# direkomendasikan untuk project Expo agar `npm install` deterministik.
legacy-peer-deps=true
