TASK ID: BL-002-02
TITLE: Validasi form produk
STATUS: TESTED

BACKLOG: BL-002 — Product Variant (master data)
REQUIREMENTS: FR-PR-002, FR-PR-003, FR-PR-005; BR-PR-002, BR-PR-003; `03-Functional-Requirements.md` FR-APP-004
ACCEPTANCE CRITERIA: AC-PR-001, AC-PR-002

SCOPE:
Validasi domain untuk Product Variant: nama wajib (maks 120 karakter), harga jual integer non-negatif,
batas low stock integer non-negatif, deskripsi opsional (maks 240 karakter).

IMPLEMENTATION:
validateProductForm di src/domain/master/validation.ts memakai helper murni dari src/domain/validation.ts.
Aturan yang sama dipakai kembali oleh ProductService sebelum menyimpan data.

FILES CHANGED:
- src/domain/master/validation.ts
- src/domain/master/types.ts (ProductFormValues, ProductValidatedValues, EMPTY_PRODUCT_FORM)
- src/domain/master/validation.test.ts

TEST:
src/domain/master/validation.test.ts, describe "validateProductForm": 4 test lulus pada reconciliation
(input valid + normalisasi rupiah, nama kosong ditolak, harga desimal ditolak, batas low stock negatif ditolak).

PREVIEW:
TIDAK BERLAKU (validasi domain murni).

VERIFICATION:
UNKNOWN — verifikasi formal AC-PR-001/AC-PR-002 pada aplikasi belum dilakukan (lihat BL-002-08).

EVIDENCE:
Output `npx jest` 2026-09-22 (suite src/domain/master/validation.test.ts PASS).

ISSUES:
Pesan error menyimpan spasi di awal pada beberapa pesan produksi (mis. " Produk wajib dipilih."),
sedangkan validasi master data tidak; perlu diseragamkan (dicatat pada BL-005-01/BL-016-02 bila disentuh).

DECISIONS:
"3.000" diterima sebagai 3000 dan koma ditolak, agar petugas tetap dapat menulis pemisah ribuan.

NOTES:
Log retrospektif dibuat 2026-09-22 saat reconciliation AGENTS.md; bersumber dari bukti repository, bukan catatan pelaksanaan asli.
